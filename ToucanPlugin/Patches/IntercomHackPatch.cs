/*using HarmonyLib;

namespace ToucanPlugin.Patches
{
    [HarmonyPatch(typeof(Intercom))]
    [HarmonyPatch(nameof(Intercom.Start))]
    internal static class IntercomPatch
    {
        private static void Prefix(Intercom __instance)
        {
            __instance._state = Intercom.State.
        }
    }
}*/